@model IEnumerable<Match.Models.Place_off_day>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_manager.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.place_off_day_number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.place_off_day1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Place.shop_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Place.place_type_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.place_off_day_number)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.place_off_day1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Place.shop_name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Place.place_type_id)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.place_off_day_number }) |
        @Html.ActionLink("Details", "Details", new { id = item.place_off_day_number }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.place_off_day_number })
    </td>
</tr>
}

</table>
