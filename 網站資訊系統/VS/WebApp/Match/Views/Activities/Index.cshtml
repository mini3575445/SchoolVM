@model IEnumerable<Match.Models.Activity>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_manager.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.activity_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Activity_type.activity_type_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activity_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Member.member_account)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activity_datetime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Place.place_type_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.activity_id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Activity_type.activity_type_name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.activity_name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Member.member_account)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.activity_datetime)
    </td>
    <td>
            @Html.DisplayFor(modelItem => item.Place.place_type_id)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.activity_id }) |
        @Html.ActionLink("Details", "Details", new { id = item.activity_id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.activity_id })
    </td>
</tr>
}

</table>
